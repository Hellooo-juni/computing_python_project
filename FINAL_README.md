# 프로젝트 이름: 까먹을 것 같은 🥺불안감😭 해소에 도움이 되는 과목별 랜덤 문제 뽑기🗃
> 여러 과목을 공부할 때 특정 과목에만 집중하다가 다른 과목을 잊을 것 같은 불안감을 줄이고, 짧은 시간에도 여러 과목을 균형 있게 복습할 수 있도록 만든 Python GUI 프로그램입니다.

## 1. 📝무엇을 하는 프로그램인가요?

사용자가 과목별 폴더에 문제 이미지와 해설 이미지를 저장해 두면, 프로그램이 선택한 과목 또는 전체 과목에서 문제를 무작위로 보여줍니다.

문제를 푼 뒤 **[다 풀었다]** 버튼을 누르면 같은 파일 이름의 해설 이미지가 나타납니다. 사용자가 **[맞혔다]** 또는 **[틀렸다]** 버튼을 누르면 오늘 푼 문제 수, 맞힌 문제 수, 정답률이 자동으로 저장됩니다.

참고로, 처음 실행할 때 사용자가 자신의 과목명을 직접 입력할 수 있으므로 누구나 이용할 수 있습니다.

---

## 2. 📌주요 기능

- 처음 실행할 때 과목명을 직접 입력하고 `subjects.json`에 저장해서 입력한 과목에 맞는 폴더 구조 자동 생성
- 과목별 문제 또는 전체 과목의 문제를 무작위로 선택
- 문제 이미지와 해설 이미지 표시 (`.png`, `.gif`, `.jpg`, `.jpeg` 등 다양한 형식의 이미지 표시 가능!)
- 큰 이미지를 GUI 창에 맞게 축소하면서 원본 비율 유지
- 맞힘/틀림 결과 기록
- 오늘 푼 문제 수, 맞힌 문제 수, 정답률 표시
- 날짜가 바뀌면 오늘의 기록 자동 초기화
- 문제 또는 해설 파일이 없을 때 안내 문구 표시

---

## 3. ✏️준비 사항과 실행 방법

## 🌟준비사항
- Python 3.10 이상 설치
- Python 기본 라이브러리: `os`, `json`, `random`, `tkinter`, `datetime`
- pip install로 추가할 라이브러리: `Pillow`

`tkinter`는 일반적인 Windows용 Python 설치에 포함되어 있으니 괜찮지만 이 프로그램은 JPG/JPEG 파일을 열고 큰 이미지의 크기를 조절해야 하니 Pillow 라이브러리를 설치해줘야 합니다!

### Pillow 설치

VS Code 터미널 또는 PowerShell에서 다음 명령어를 입력합니다.

```powershell
pip install pillow
```

위 명령어가 실행되지 않으면 다음 명령어를 사용합니다.

```powershell
python -m pip install pillow
```

Python 실행 명령어가 `py`인 환경에서는 다음과 같이 설치할 수도 있습니다.

```powershell
py -m pip install pillow
```

---

## 🌟실행 방법

### 1) VS Code에서 `step4_final_random_question_program.py`을 실행.
### 2) 처음 실행할 때 과목 입력하기

처음 실행하면 과목 입력창이 나타납니다. 과목명을 쉼표로 구분하여 입력합니다.

```text
진동공학, 수치해석, 시스템제어
```

입력한 과목은 `subjects.json`에 저장되므로 두 번째 실행부터는 다시 입력하지 않아도 됩니다.

### 3) 자동 생성된 폴더에 이미지 넣기

프로그램을 한 번 실행하면 다음과 같은 구조가 자동으로 만들어집니다.

```text
study_questions/
├─ 진동공학/
│  ├─ problems/
│  └─ answers/
├─ 수치해석/
│  ├─ problems/
│  └─ answers/
└─ 시스템제어/
   ├─ problems/
   └─ answers/
```

각 과목의 `problems` 폴더에는 문제 이미지를, `answers` 폴더에는 해설 이미지를 넣습니다.

문제와 해설은 반드시 **같은 파일 이름과 같은 확장자**를 사용해야 합니다.

```text
study_questions/진동공학/problems/q001.jpg
study_questions/진동공학/answers/q001.jpg
```

지원 형식은 다음과 같습니다.

```text
.png  .gif  .jpg  .jpeg
```

### 4) 프로그램 다시 실행하기

이미지를 넣은 뒤 프로그램을 다시 실행하고 과목 버튼을 누르면 랜덤 문제가 표시됩니다.

1. 과목 또는 **전체 과목 랜덤** 선택
2. 문제 풀이
3. **다 풀었다** 버튼으로 해설 확인
4. **맞혔다** 또는 **틀렸다** 선택
5. **다음 문제** 버튼으로 새 문제 불러오기

---

## 4. 🖊️생성되는 파일

| 파일 또는 폴더 | 역할 |
|---|---|
| `study_questions/` | 과목별 문제와 해설 이미지를 저장하는 상위 폴더 |
| `subjects.json` | 처음 입력한 과목 목록 저장 |
| `today_record.json` | 날짜, 오늘 푼 문제 수, 맞힌 문제 수 저장 |

참고로 `make_folders()` 함수는 프로그램을 실행할 때마다 폴더 생성 명령을 수행하지만, `exist_ok=True`를 사용합니다. 따라서 이미 폴더가 있으면 덮어쓰거나 삭제하지 않고 그대로 사용하며, 저장해 둔 문제와 해설 이미지도 유지됩니다.

---

## 5. ✨사용된 Python 개념

- **리스트**: 과목 목록과 이미지 파일 목록 저장
- **딕셔너리**: 하나의 문제 정보와 오늘의 학습 기록 저장
- **함수**: 과목 불러오기, 폴더 만들기, 랜덤 문제 선택, 이미지 표시, 기록 저장 등 기능 분리
- **조건문**: 과목 선택 여부, 파일 존재 여부, 날짜 변경 여부 확인
- **반복문**: 과목별 폴더와 과목 버튼 생성, 이미지 파일 탐색
- **파일 입출력**: JSON 파일 읽기와 쓰기
- **예외 상황 처리**: 문제 또는 해설이 없을 때 프로그램 종료 대신 안내 문구 표시
- **GUI**: `tkinter`를 이용한 버튼, 안내 문구, 이미지 영역 구성
- **외부 라이브러리**: Pillow의 `Image`, `ImageTk`를 이용한 이미지 열기와 크기 조절

---

## 6. 😎설계에서 최종 코드까지 바뀐 점

### 고정된 과목 목록을 사용자 입력 방식으로 변경

step3 단계까지는 미리 입력해둔 특정 수강 과목이 코드 안의 리스트에 고정되어 있었습니다. 이 방식은 다른 사용자가 코드를 수정하지 않으면 사용할 수 없다는 문제가 있었습니다.

그래서 최종 코드에서는 처음 실행할 때 과목명을 입력받고 `subjects.json`에 저장하도록 바꾸었습니다. 이후 실행부터는 저장된 과목 목록을 자동으로 불러오게 됩니다.

### 사용자가 직접 폴더를 만들던 방식에서 자동 생성 방식으로 변경

처음에는 사용자가 실행 전에 `study_questions`, `problems`, `answers` 폴더를 직접 만들도록 안내하려 했습니다. 그러나 프로그램이 처리할 수 있는 일을 사용자에게 맡기는 방식이라고 판단했습니다.

그래서 최종 코드에서는 `os.makedirs(..., exist_ok=True)`를 사용하여 필요한 폴더를 자동 생성합니다. 이미 폴더가 있어도 기존 파일을 지우지 않습니다.

### 이미지 형식과 크기 문제 해결

step3 코드에서는 `tk.PhotoImage`를 사용했기 때문에 주로 PNG와 GIF만 안정적으로 처리할 수 있었고, 큰 이미지는 GUI에서 제대로 보이지 않았습니다.

그래서 최종 코드에서는 Pillow를 추가하여 JPG와 JPEG도 지원하도록 했습니다. 또한 `thumbnail()`을 사용해 이미지를 최대 `760 × 420` 영역 안에 들어오도록 줄이되 원본 가로세로 비율은 유지하도록 개선했습니다.

---

## 7. 🫶AI 도구 사용 내역

AI는 프로젝트 전체를 한 번에 대신 작성하도록 사용하지 않고, 이 프로젝트의 특정 부분에서 아직 배우지 않은 범위의 내용을 요해서 도저히 어떻게 코드를 짜야할지 모르겠을 때 도움을 받았습니다. 그리고 AI가 준 코드를 그대로 붙여 넣기보다는 제 기존 코드에서 어느 부분을 바꿔야 하는지 이해한 뒤 수정했습니다. 

구체적으로 다음과 같이 활용했습니다.

- 큰 이미지가 보이지 않는 문제를 해결할 수 있는 방법을 여러 가지로 비교함. 이때 `tk.PhotoImage`와 Pillow 방식의 차이를 확인함.
- 과목 목록을 코드에 고정하지 않고 사용자에게 입력받는 방법을 질문함.
- 입력받은 과목 목록을 토대로 자동으로 폴더를 만들어내는 기능을 구현하는 방법을 질문함. 이때 AI가 제안한 함수인 os.makedirs()가 뭔지, 그 함수 안의 키워드 인자 `exist_ok=True`의 의미 등을 질문함.

그리고 코드를 전체적으로 완성한 뒤엔, 코드 각 줄의 역할을 정리해서  발표와 질의응답에서 설명할 수 있을만큼 공부하기 위해 활용했습니다.

---

## 8. 🤔한계와 다음 단계

현재 버전은 하루 전체 기록만 저장하기 때문에 과목별 정답률을 확인할 수 없습니다. 또한 틀린 문제만 자동으로 모아 다시 출제하거나, 사용 중에 과목을 추가·삭제하는 기능은 없습니다.

향후에는 다음 기능을 추가하고 싶습니다.

- 과목별 푼 문제 수와 정답률 저장
- 틀린 문제만 다시 출제하는 오답 복습 모드
- 주간·월간 학습량 그래프
- 프로그램 안에서 과목 추가 및 삭제
- 이미지 확대와 축소
- 같은 문제가 연속해서 나오는 현상 방지


---
