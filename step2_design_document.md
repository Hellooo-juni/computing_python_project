# 프로젝트 이름: 까먹을 것 같은 🥺불안감😭 해소에 도움이 되는 과목별 랜덤 문제 뽑기🗃 
### - 2024013156 기계공학부 정훈준

<2단계 설계 문서 요약>: 
이 프로그램은 시험기간에 여러 과목을 균형 있게 복습하고 싶은 학생을 위한 과목별 랜덤 문제 출제 프로그램이다. 사용자는 과목별 폴더에 문제 이미지와 해설 이미지를 미리 저장해 둔다. 프로그램은 사용자가 선택한 과목 또는 전체 과목에서 문제를 랜덤으로 불러와 화면에 보여준다. 사용자가 문제를 푼 뒤 [다 풀었다] 버튼을 누르면 같은 파일 이름을 가진 해설 이미지가 나타난다. 이후 사용자가 [맞혔다] 또는 [틀렸다] 버튼을 누르면 오늘 푼 문제 수, 맞힌 문제 수, 정답률이 자동으로 기록된다.

이 설계는 처음부터 모든 기능을 완벽하게 만들기보다는, 먼저 문제 랜덤 출력, 해설 확인, 맞힘/틀림 기록이라는 핵심 기능을 구현한 뒤 나중에 과목별 통계나 틀린 문제 다시 풀기 기능을 추가할 수 있도록 구성하였다.

# 1. <기능 목록 (MUST/NICE/LATER 표시>
### 프로그램이 해야 할 일(:동사+목적어 형태)과 우선순위: 반드시(MUST), 있으면 좋음(NICE), 시간 남으면(LATER)

|우선순위| 기능 | 설명 |
|:---:|:---:|:---:|
|MUST|과목 목록을 보여주기|사용자가 복습할 과목을 선택할 수 있도록 과목 목록을 화면에 보여준다.|
|MUST|과목을 선택하기|사용자가 선택한 과목 폴더에서 문제를 불러올 수 있도록 한다.|
|MUST|문제 이미지를 랜덤으로 불러오기|선택한 과목의 problems 폴더에서 문제 이미지를 무작위로 선택한다.|
|MUST|문제 이미지를 화면에 표시하기|랜덤으로 선택된 문제 이미지를 GUI 화면에 보여준다.|
|MUST|다 풀었다 버튼을 만들기|사용자가 문제를 다 푼 뒤 해설을 볼 수 있도록 버튼을 제공한다.|
|MUST|해설 이미지를 보여주기|문제 이미지와 같은 이름의 해설 이미지를 answers 폴더에서 찾아 화면에 보여준다.|
|MUST|맞힘/틀림을 입력받기|사용자가 해설을 본 뒤 스스로 맞혔는지 틀렸는지 버튼으로 입력한다.|
|MUST|오늘 푼 문제 수를 저장하기|사용자가 맞힘 또는 틀림을 누르면 오늘 푼 문제 수를 1 증가시킨다.|
|MUST|오늘 맞힌 문제 수를 저장하기|사용자가 맞혔다 버튼을 누르면 오늘 맞힌 문제 수를 1 증가시킨다.|
|NICE|오늘 정답률을 계산하기|오늘 맞힌 문제 수를 오늘 푼 문제 수로 나누어 정답률을 계산해 보여준다.|
|NICE|전체 과목 중 랜덤 뽑기|특정 과목이 아니라 전체 과목 중에서 문제를 무작위로 뽑을 수 있게 한다.|
|NICE|날짜가 바뀌면 기록을 초기화하기|저장된 날짜와 오늘 날짜가 다르면 solved와 correct 값을 0으로 초기화한다.|
|NICE|파일이 없을 때 안내 메시지를 보여주기|문제 이미지나 해설 이미지가 없을 경우 오류가 아니라 안내 문구를 보여준다.|
|LATER|과목별 정답률을 저장하기|전체 기록뿐 아니라 과목별로 푼 문제 수와 맞힌 문제 수를 따로 저장한다.|
|LATER|틀린 문제를 다시 푸는 기능|틀렸던 문제만 따로 모아서 다시 랜덤으로 출제한다.|
|LATER|주간/월간 학습 통계를 보기|하루 기록뿐 아니라 일주일 또는 한 달 단위의 학습량을 그래프로 보여준다.|
|LATER|문제 이미지를 확대/축소하는 기능|화면에서 문제 이미지가 작거나 클 때 사용자가 크기를 조절할 수 있도록 한다.|


# 2. <데이터 구조 설계 (어떤 변수에 무엇이 들어가는지)>

## 1) 과목 목록 데이터: 과목 이름은😊리스트😊에 저장한다. 프로그램 시작 화면에서 과목 버튼을 만들 때 사용한다.
```python
subjects = [
    "진동공학",
    "계측공학",
    "수치해석",
    "자연과학 코딩기초",
    "시스템제어",
    "기계요소설계",
    "컴퓨팅사고와 SW코딩"
]
```
## 2) 하나의 문제와 그 해설을 나타내는 데이터 구조: 
#### 일단, 문제 이미지는 각 과목 폴더 안의 problems 폴더에 저장하고, 해설 이미지는 같은 과목 폴더 안의 answers 폴더에 저장한다. 문제 이미지와 해설 이미지는 꼭 같은 파일 이름을 사용해줘야한다. 예를 들어 문제 이미지가 q001.png이면, 해설 이미지도 q001.png로 저장한다.
```
study_questions/
│
├─ 진동공학/
│   ├─ problems/
│   │   ├─ q001.png
│   │   ├─ q002.png
│   │   └─ q003.png
│   └─ answers/
│       ├─ q001.png
│       ├─ q002.png
│       └─ q003.png
│
├─ 계측공학/
│   ├─ problems/
│   └─ answers/
│
├─ 수치해석/
│   ├─ problems/
│   └─ answers/
```
이런 폴더 구조로 사진들을 저장해두면,
## 프로그램 안에서는 각 문제를😊딕셔너리😊로 저장할 수 있다.
```python
question = {
    "subject": "진동공학",
    "problem_path": "study_questions/진동공학/problems/q001.png",
    "answer_path": "study_questions/진동공학/answers/q001.png",
    "file_name": "q001.png"
}
```
각 항목의 의미는,
|key|의미|
|:---:|:---:|
|subject|문제의 과목 이름|
|problem_path|문제 이미지 파일 경로|
|answer_path|해설 이미지 파일 경로|
|file_name|문제와 해설을 연결하는 파일 이름|

## 3) 오늘의 학습 기록 데이터 구조
### 오늘 푼 문제 수와 맞힌 문제 수는😊JSON😊파일에 저장한다. 날짜가 오늘 날짜와 다르면 solved와 correct를 다시 0으로 초기화한다!
```json
{
  "date": "2026-05-17",
  "solved": 12
  "correct": 8
}
```
각 항목의 의미는,
|key|의미|
|:---:|:---:|
|date|기록이 저장된 날짜|
|solved|오늘 푼 문제 수|
|correct|오늘 맞힌 문제 수|
# 3. <함수별 의사코드(3~5개 함수)>
### 함수 1. load_record()
>역할: 저장된 오늘의 학습 기록을 불러온다. 만약 기록 파일이 없거나 날짜가 오늘과 다르면 새 기록을 만든다.  
>입력: 없음  
>출력: 오늘의 학습 기록 딕셔너리

의사코드:
```python
def load_record():
    오늘 날짜를 구한다

    if today_record.json 파일이 없다면:
        새 기록 딕셔너리를 만든다
        date는 오늘 날짜로 저장한다
        solved는 0으로 저장한다
        correct는 0으로 저장한다
        새 기록을 반환한다

    파일에서 기록을 읽어온다

    if 기록에 저장된 날짜가 오늘 날짜와 다르다면:
        solved와 correct를 0으로 초기화한다
        date를 오늘 날짜로 바꾼다

    기록을 반환한다
```
### 함수 2. save_record(record)
>역할: 현재 학습 기록을 JSON 파일에 저장한다.  
>입력: record: 오늘의 학습 기록 딕셔너리  
>출력: 없음

의사코드:
```
def save_record(record):
    today_record.json 파일을 쓰기 모드로 연다
    record 딕셔너리를 JSON 형식으로 저장한다
```

### 함수 3. get_random_question(subject)
>역할: 사용자가 선택한 과목의 문제 폴더에서 문제 이미지를 랜덤으로 하나 선택한다.  
>입력: subject: 사용자가 선택한 과목 이름  
>출력: 선택된 문제 정보 딕셔너리

의사코드:
```python
def get_random_question(subject):
    if subject가 "전체 과목 랜덤"이라면:
        전체 과목의 problems 폴더를 모두 확인한다
        모든 문제 이미지 목록을 하나의 리스트에 모은다

    else:
        선택한 과목의 problems 폴더를 확인한다
        해당 폴더 안의 문제 이미지 목록을 만든다

    if 문제 이미지 목록이 비어 있다면:
        문제 파일이 없다는 안내를 한다
        None을 반환한다

    문제 이미지 목록에서 하나를 랜덤으로 고른다

    고른 문제 파일 이름과 같은 이름의 해설 이미지 경로를 만든다

    question 딕셔너리를 만든다
    question을 반환한다
```

### 함수 4. show_problem(question)
>역할: 선택된 문제 이미지를 GUI 화면에 보여준다.  
>입력: question: 문제 정보 딕셔너리  
>출력: 없음

의사코드:
```python
def show_problem(question):
    question 딕셔너리에서 problem_path를 꺼낸다
    문제 이미지를 불러온다
    GUI 화면의 이미지 영역에 문제 이미지를 표시한다
    "다 풀었다" 버튼을 활성화한다
```

### 함수 5. show_answer(question)
>역할: 사용자가 “다 풀었다” 버튼을 누르면 해설 이미지를 보여준다.  
>입력: question: 문제 정보 딕셔너리  
>출력: 없음

의사코드:
```python
def show_answer(question):
    question 딕셔너리에서 answer_path를 꺼낸다

    if answer_path에 해당하는 해설 이미지 파일이 없다면:
        해설 파일이 없다는 안내 메시지를 보여준다
        return

    해설 이미지를 불러온다
    GUI 화면의 이미지 영역에 해설 이미지를 표시한다
    "맞혔다" 버튼과 "틀렸다" 버튼을 활성화한다
```

### 함수 6. update_result(is_correct)
>역할: 사용자가 맞혔다 또는 틀렸다를 누르면 오늘의 학습 기록을 업데이트한다.  
>입력: is_correct: 맞혔으면 True, 틀렸으면 False  
>출력: 없음

의사코드:
```python
def update_result(is_correct):
    solved 값을 1 증가시킨다

    if is_correct가 True라면:
        correct 값을 1 증가시킨다

    정답률을 계산한다
    기록을 JSON 파일에 저장한다
    화면에 오늘 푼 문제 수, 맞힌 문제 수, 정답률을 출력한다
```

# 4. 화면 흐름 스케치 (손그림 사진 OK)
### 1. 과목 선택 화면 
사용자는 이 화면에서 오늘 복습할 과목을 선택한다.
```
====================================
       복습할 과목을 선택하세요
====================================

[진동공학]
[계측공학]
[수치해석]
[자연과학 코딩기초]
[시스템제어]
[기계요소설계]
[컴퓨팅사고와 SW코딩]
[전체 과목 랜덤]
```
### 2. 문제 풀이 화면
사용자가 문제를 충분히 푼 뒤 [다 풀었다] 버튼을 누르면 해설 화면으로 넘어간다.
```
====================================
          진동공학 랜덤 문제
====================================

[          문제 이미지 표시 영역          ]



              [다 풀었다]
```
### 3. 해설 확인 화면
사용자는 해설 이미지를 보고 자신이 맞혔는지 틀렸는지 직접 판단해서 버튼을 누른다.
```
====================================
             해설 확인
====================================

[          해설 이미지 표시 영역          ]



        [맞혔다]        [틀렸다]
```
### 4. 오늘 기록 화면
기록을 확인한 뒤 [다음 문제 풀기] 버튼을 누르면 다시 랜덤 문제가 나온다.
```
====================================
             오늘의 학습 기록
====================================

오늘 푼 문제: 12개
오늘 맞힌 문제: 8개
오늘 정답률: 66.7%

              [다음 문제 풀기]
```